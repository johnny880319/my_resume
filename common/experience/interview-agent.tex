% !TEX root = ../../variants/genai/main.tex
        \resumeItem{\textbf{AI Interview Agent (GAI Recruiter)}: Architected a real-time streaming speech recognition pipeline (currently in beta rollout) using \textbf{Python (asyncio), Ray, and WebSockets} to serve \textbf{multiple concurrent interview sessions}. Implemented a \textbf{sliding-window approach} to sustain \textbf{sub-500 ms streaming latency}. Tuned VAD parameters to significantly reduce hallucinations.}
